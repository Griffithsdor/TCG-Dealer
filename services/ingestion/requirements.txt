# Dependencias del servicio de ingesta
pyyaml>=6.0
httpx>=0.27      # cliente HTTP para los adaptadores de proveedores
psycopg[binary]>=3.1
# tcg-core se instala desde ../../packages/core (pip install -e packages/core)
